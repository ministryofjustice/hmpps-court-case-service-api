nomis-oauth:
  base-url: https://sign-in-dev.hmpps.service.justice.gov.uk/auth

spring:
  r2dbc:
    url: r2dbc:pool:postgresql://${RDS_INSTANCE_ENDPOINT}/${DATABASE_NAME}?schema=${database.schema.name}&stringtype=unspecified&sslmode=require
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}