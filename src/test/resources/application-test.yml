server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

hmpps-auth:
  url: "http://localhost:8090/auth"

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/hmpps_court_case_service
    username: admin
    password: password
